#include <stdio.h>

// Traverses a given array, applying "fp" to every element
void traverse(unsigned int a[], int len, void (*fp) (unsigned int));
// Prints an unsigned integer as hexadecimal
void printAsHex (unsigned int n);
// Prints an unsigned integer halved
void printHalved(unsigned int n);

// prints all elements of an array in hexadecimal
void printAllAsHex (unsigned int a[], int len);
// prints all elements of an array halved
void printAllHalved (unsigned int a[], int len);

int main (void) {
    // int arr[5] = {1, 5, 389, 4214, 8};
    // printAllAsHex(arr, 5);
    // printAllHalved(arr, 5);
}

// Traverses a given array, applying "fp" to every element
void traverse(unsigned int a[], int len, void (*fp) (unsigned int)) {
    // TODO: Complete this function
}

// Prints an unsigned integer as hexadecimal
void printAsHex (unsigned int n) {
    // TODO: Complete this function
}
// Prints an unsigned integer halved
void printHalved(unsigned int n) {
    // TODO: Complete this function
}

// prints all elements of an array in hexadecimal
void printAllAsHex (unsigned int a[], int len) {
    // TODO: Complete this function
}
// prints all elements of an array halved
void printAllHalved (unsigned int a[], int len) {
    // TODO: Complete this function
}
